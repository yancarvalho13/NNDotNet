# NNDotNet

Uma rede neural simples, criada apenas para entender na pratica como funciona um MLP (Multilayer Perceptron).

O projeto implementa o treinamento e o backpropagation sem bibliotecas de machine learning, usando C# e o dataset MNIST.
